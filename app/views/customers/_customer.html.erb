<div class="card">
  <div class="card-body">
    <h5 class="card-title"><%= customer.name %></h5>
    <p class="card-text">Data de nascimento: <%= customer.dob %></p>
    <p class="card-text">Telefone: <%= customer.mobile_phone %></p>
    <div class="card-footer text-center">
      <%= link_to 'Editar', edit_customer_path(customer), class: "btn btn-primary" %>
      <%= link_to 'Excluir', customer, method: :delete, data: { confirm: 'Você tem certeza?' }, class: "btn btn-danger" %>
    </div>
  </div>
</div>
